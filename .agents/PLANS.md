# .agents/PLANS.md

## 現在の進捗
- LPの活用シーンに `assets` 配下の生成済みモックアップ画像を挿入済み。
- 最適化済み `image1.webp` 〜 `image4.webp` を業種に合わせて正しい順序へ調整済み。
- `.agents/CHANGELOG.md` と `VERSION` を追加し、変更履歴の運用ルールを初期化済み。
- PC/スマホ表示、画像4枚の読み込み、横スクロール、FAQの0.4秒アニメーションを確認済み。
- 使い方ステップのアイコンをタイトル横へ移動し、`.scene-asset` の背景・枠線・影を削除済み。
- ブラウザ上の計算済みスタイルで、アイコン横並びと `.scene-asset` の装飾削除を確認済み。
- STEPラベルを改行表示にし、ステップ矢印の余白と「テラコッタでできること」の画像比率を調整済み。
- PC/スマホ幅でSTEP改行、アイコン横並び、機能画像16:9、横スクロールなしを確認済み。
- スマホ版ファーストビュー画像を画面横幅いっぱいに近づける調整を追加済み。
- 390px幅でFV画像が画面幅いっぱいに表示され、横スクロールが出ないことを確認済み。
- FV画像 `.fv-image-graph` の `aspect-ratio` を `1` に変更済み。
- PC/390px幅でFV画像が正方形表示になり、横スクロールが出ないことを確認済み。

## 学び
- 生成済みassetは、各業種のスマホモックアップと分析カードが一体化しているため、CSS製の仮モックよりもLP内で意図が伝わりやすい。
- `.agents` ディレクトリは未作成だったため、今後の運用に必要なドキュメント一式を最小構成で用意した。

## 次の手
- 次回変更時は、作業前に `.agents` 配下の設計・計画・テスト方針を読み直す。
